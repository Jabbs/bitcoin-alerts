<%= link_to "/channels/#{channel.id}/subscriptions/#{current_user.subscriptions.find_by_channel_id(channel.id).try(:id)}", method: :delete, remote: true, class: "subscription-create-link", title: "Turn Off Notification" do %>
  <%= image_tag('on_switch.png') %>
<% end %>
